# 前処理結果レポート: 全国中学生人権作文コンテスト テキスト解析準備

## 1. プロジェクト概要
本工程では、全国中学生人権作文コンテストの過去の受賞作をテキスト解析するため、PDFから変換されたテキストデータを構造化し、ベクトル化（Embedding）して SQLite データベースに格納する前処理を実施した。

## 2. 指示および要求仕様
### 入力データ
- `essay-text` ディレクトリ内の `.txt` および `.md` ファイル（PDFから変換済み）。
- ファイル名に年度情報が含まれている。

### 抽出・保存項目
- **年号**: ファイル名より抽出。
- **賞の名前**: LLM により抽出。
- **タイトル**: LLM により抽出。
- **居住県**: LLM により抽出。
- **所属学校**: LLM により抽出。
- **学年**: LLM により抽出。
- **本文**: 原文を保持。
- **原文**: 検証用にファイル内容をそのまま保持。
- **ベクトル**: タイトルおよび本文の Embedding。

### 技術スタック
- **Runtime**: Node.js v22
- **Database**: SQLite (better-sqlite3)
- **LLM / Embedding**: LM Studio
    - LLM Model: `google/gemma-4-e4b`
    - Embedding Model: `text-embedding-bge-m3`

### 制約事項
- 筆者の氏名・フリガナ等の個人情報は DB に保存しない。

## 3. 実装内容および修正履歴

### 3.1 構造化抽出フローの最適化
当初は本文 (`body`) も LLM に抽出させていたが、以下の問題が発生した。
- **パフォーマンス低下**: 長文の処理によるレスポンス時間の増大。
- **パースエラー**: 本文中の制御文字や特殊文字による JSON 形式の破壊 (`Unexpected token`, `Bad control character`)。

**【修正】**: LLM にはメタデータ（賞、タイトル、県、学校、学年）のみを抽出させ、本文はファイルから読み込んだ原文をそのまま `body` 列に格納する方式に変更した。

### 3.2 テキストクレンジングの導入
PDF 変換時に混入した不可視の制御文字が DB 操作や解析の妨げになることを防ぐため、以下の処理を導入した。
- **制御文字の除去**: 改行、タブを除く `\x00-\x1F` などの制御文字を削除する `cleanText` 処理を実装し、`body` 列への格納前に適用した。

### 3.3 Embedding 戦略の変更
解析の柔軟性を高めるため、単一のベクトル保存から分離保存へ変更した。
- **分離保存**: `title_embedding` と `body_embedding` の 2 列を設け、それぞれ個別にベクトルを算出。
- **PDF ノイズ除去**: 本文のベクトル化直前に、すべての空白・改行を削除する正規化処理を追加。これにより、右端折り返しによる改行などの PDF 由来のノイズがベクトルに影響を与えないようにした。
- **例外処理**: タイトルと本文の両方が空であるレコード（本文非公開作品など）を自動的にスキップするロジックを追加。

## 4. 実行結果

### 4.1 パイプライン実行状況
- **DB初期化 (`npm run init-db`)**: 正常完了。`essays` テーブルを作成。
- **インポート (`npm run import`)**: 正常完了。全ファイルからメタデータを抽出し、本文および原文を DB に格納。
- **検証 (`npm run verify`)**: 正常完了。LLM が抽出したタイトル等の情報が原文内に正しく存在することを確認し、忠実性を担保した。
- **ベクトル化 (`npm run embed`)**: 正常完了。タイトルと本文それぞれに `text-embedding-bge-m3` によるベクトルを算出し、DB に格納。

※人間による加筆：raw_text から余分な情報を取り除き body にする作業は約30分程度。
　Claude CodeによるさくらのAI Engineへのリクエスト回数は46回。

## 5. 最終的なデータ構造 (`essays.db`)
| カラム名 | 型 | 内容 |
| :--- | :--- | :--- |
| `id` | INTEGER | プライマリキー |
| `year` | INTEGER | 年度 |
| `award` | TEXT | 受賞した賞の名前 |
| `title` | TEXT | 作品タイトル |
| `prefecture` | TEXT | 筆者の居住県 |
| `school` | TEXT | 所属学校 |
| `grade` | TEXT | 学年 |
| `body` | TEXT | クレンジング済み本文 (後で人間が調整可能) |
| `raw_text` | TEXT | ファイル原文 |
| `title_embedding`| TEXT | タイトルのベクトルデータ (JSON) |
| `body_embedding` | TEXT | 本文のベクトルデータ (JSON) |
